export declare class InterceptorConfig {
    ignoreLoading: boolean;
    ignoreErrors: boolean;
    resendRequest: boolean;
    constructor(ignoreLoading?: boolean, ignoreErrors?: boolean, resendRequest?: boolean);
}
